{
  "name": "mocks",
  "version": "1.0.0",
  "description": "Mocks Server for fyllut-ettersending",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "@mocks-server/main": "^4.1.0"
  },
  "scripts": {
    "mocks": "yarn && mocks-server",
    "mocks:no-cli": "yarn && mocks-server --no-plugins.inquirerCli.enabled"
  }
}
